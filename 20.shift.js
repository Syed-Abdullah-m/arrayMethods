// shift() removes the first elemnt from an array and reuturns it.

const arr = ["hi", "Hello", "Hola", 2, 4, 5, 6, 7];

const firstEle = arr.shift();
console.log(firstEle); // shows the first element of an array
console.log(arr); // prints the updated array

console.log(arr.shift());
console.log(arr);

console.log(arr.shift());
console.log(arr);

