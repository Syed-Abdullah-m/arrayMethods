// pop() removes the last elemnt from an array and reuturns it.


const arr = [1,23,3,4,7,95,4,34, 'abd', 'ba','nana'];

const lastEle = arr.pop();
console.log(lastEle); // shows the last element of the array
console.log(arr); // prints the updated array ( that is without the last element)

console.log(arr.pop());
console.log(arr);

console.log(arr.pop());
console.log(arr);