// The sort() method sorts the elements of an array in place and returns the sorted array.
// By default, it sorts elements as strings in ascending order,
// which can sometimes give unexpected results when sorting numbers.

// syntax: array.sort(compareFunction)
 
// let a = " - 2" + 3
// console.log(a);

function isEligibleForDiscount(age) {
    isEligibleForDiscount = age >= 18 ? return true : return "confirm (Are you elegible for a movie ticket discount?) "
}